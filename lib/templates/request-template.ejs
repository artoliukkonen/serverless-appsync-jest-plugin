{
  "version": "2017-02-28",
  "operation": "Invoke",
  "payload": {
    "field": "<%= field %>",
    "id": $util.toJson($ctx.args.id),
    "input": $util.toJson($ctx.args.input),
    "identity": $util.toJson($context.identity)
  }
}