const jestPlugin = require('serverless-appsync-jest-plugin');
const mod = require('./../handler');

const { lambdaWrapper } = jestPlugin;
const wrapped = lambdaWrapper.wrap(mod, { handler: 'request' });

const event = {
  field: '<%= resolverName %>',
};

describe('<%= resolverName %>', () => {
  const response = wrapped.run(event);

  it('function exists', () => {
    expect(response).not.toBe('Unknown function');
  });
  it('implement tests here', () => {
    expect(response).toBeDefined();
  });
});
